using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Molding : MonoBehaviour
{
    /*
    public int current_Cast;
    // Update is called once per frame
    public void CastType()
    {
        if (current_Cast <= 4)
        {
            current_Cast++;
        }
        if (current_Cast == 4)
        {
            current_Cast = 1;
        }
    }
    private void OnTriggerEnter(Collider other)
    {
        if (other.tag == "Ore")
        {
            if (other.GetComponent<Ore>().current_Stage == 2 && current_Cast ==1)
            {
                other.GetComponent<Ore>().current_Weapon_Type = "Dagger";
                other.GetComponent<Ore>().time_Hit_Needed = 1;
            }
            if (other.GetComponent<Ore>().current_Stage == 2 && current_Cast == 2)
            {
                other.GetComponent<Ore>().current_Weapon_Type = "Sword";
                other.GetComponent<Ore>().time_Hit_Needed = 2;
            }
            if (other.GetComponent<Ore>().current_Stage == 2 && current_Cast == 3)
            {
                other.GetComponent<Ore>().current_Weapon_Type = "Hammer";
                other.GetComponent<Ore>().time_Hit_Needed = 3;
            }
            other.GetComponent<Ore>().current_Stage = 3;
            print(other.GetComponent<Ore>().current_Weapon_Type);
            print("Stage2Done");
        }
    }
    */
}
